#!/bin/sh

docker compose down --rmi all --volumes --remove-orphans

rm -rf tmp/cache/*
rm -f tmp/pids/* log/*
mkdir -p tmp/{cache,pids,sessions,sockets} log

docker compose build --no-cache --pull
docker ps -a | grep qawamis
